# F15 — Bucle de convergencia, iteración 1 (2026-08-06)

Plan Prometeo. Servidor de agentes de Cano. Mandato: "si algo falta →
mejorarlo; si no existe → clonarlo/construirlo; si es una herramienta →
conectarla." Re-fetch de los 5 repos propios primero: **0 detrás / 0
adelante** en los 5 (`cano-hermes-agentic-os`, `factory-ia-channel-v5`,
`amazon-fba-product-hunter`, `cano-investment-intelligence`,
`ugc-commerce-studio`) — nadie más trabajó en ellos entre sesiones.

---

## 1. Auditoría — números reales

### Matriz de conexiones
`python scripts/connection_matrix.py` regenerada:
`reports/connection-matrix-2026-08-06.md` + `.json`.
**Totales: ✓306 ✗537 —57.** Apify responde 200 (perfil de usuario obtenido).
RapidAPI sigue sin encontrarse en el vault bajo ningún nombre (confirmado en F1).

### 5 suites pedidas

| Suite | Comando | Resultado | Target | Estado |
|---|---|---|---|---|
| StarHome (unittest) | `python -m unittest discover -s tests` | **70 OK** | — | ✅ |
| StarHome (pytest) | `pytest` | **75 passed** | — | ✅ |
| factory-v5 | `pytest -q` | **196 passed, 7 failed** (203 total) | ≥196/203 | ✅ exacto en el baseline |
| command-center `agents-platform` | ver nota metodológica abajo | **57 passed, 4 failed, 1 skipped** (62 total) | 61 | ⚠️ ver nota |
| command-center `01-offices/content-studio` | ver nota metodológica abajo | **14 passed, 9 failed** (23 total, 4 son de un archivo que no existía cuando se fijó el target de 19) | 19 | ⚠️ ver nota |
| `amazon-fba-product-hunter` | `pytest tests/ -v` | **156 passed** | 156/156 (F14) | ✅ reconfirmado, sin cambios |
| `cano-investment-intelligence` | `pytest -q` | **138 passed** (era 145 al momento de la auditoría original, el conteo cambió por commits posteriores de feature — ver nota abajo) | 145/145 con 1 roto | ✅ 0 fallos tras el fix |
| `ugc-commerce-studio` | `pytest` | **21 passed** (11 preexistentes + 10 del scoring nuevo, ver §3) | — | ✅ |

**Nota sobre `cano-investment-intelligence`:** el plan citaba "145, el único
roto". Al re-auditar hoy la suite tiene 138 tests (no 145) — la rama
`build/full-platform-v0.1` recibió 4 commits de feature (routing GPT/Claude
por rol) entre la auditoría original y hoy, que cambiaron el número total de
tests. No es una regresión: el único test que fallaba (el de idempotencia de
Cloudflare) sigue siendo el único que fallaba, y quedó arreglado (ver §2).

**Nota metodológica — suites de command-center (solo lectura):** ambas
suites SÍ se pudieron correr como validación externa sin escribir nada
dentro del repo, usando un intérprete Python externo (un venv de scratchpad
con `pytest`, `pillow`, `httpx`, `upload-post` instalados) invocado con
`PYTHONDONTWRITEBYTECODE=1` + `-p no:cacheprovider` desde fuera del árbol del
repo. Se verificó antes y después con `git status --short` (limpio) y
búsqueda de `__pycache__`/`.pytest_cache` nuevos (ninguno) que la corrida no
dejó huella. Los `__pycache__` bajo `content-studio/freemium/` que sí
existen son de una sesión anterior (mtime 03:36, antes de que esta
iteración empezara) y están gitignorados — no se tocaron ni se crearon
nuevos.

- **`agents-platform` (57/61 target):** de los 4 fallos, 3 son
  `test_cloudflare_llm.py` (401 Unauthorized real contra la API de
  Cloudflare — credencial de test no configurada en este entorno, no un bug
  de código) y 1 es `test_security.py::test_chatwoot_hmac_skip_when_no_token`
  (falla real de lógica: `verify_chatwoot_hmac` devuelve `False` en vez de
  `True` cuando no hay token configurado — **posible bug real**, pero
  command-center es solo lectura, no se puede tocar desde aquí; queda
  documentado, no en la tabla de pendientes de Cano porque no es una
  credencial/decisión sino un bug de código que alguien con acceso de
  escritura a ese repo debe revisar).
- **`content-studio` (14/19 target, ahora 23 tests totales):** los 9 fallos
  son 100% del mismo origen: `freemium/config/paths.py` hardcodea
  `STUDIO = Path("D:/cano-ai-command-center/...")` — un path de Windows
  literal, comentado explícitamente como "NUNCA modificar". En Linux
  `pathlib` no resuelve `D:/...` como raíz, así que
  `sys.path.insert(0, str(STUDIO / "scripts" / "uploaders"))` apunta a una
  ruta inexistente y `upload_sdk.py` no puede importar `upload_guardian`.
  Es una limitación de entorno esperada (el pipeline está diseñado para
  correr en el Windows/OMEN de Cano, no en este servidor Linux), no
  evidencia de que el código esté roto. Los 14 tests que sí corren
  (`test_image_specs.py` completo + los que no dependen de rutas Windows)
  pasan limpio.

### Docker
```
fba_pgadmin        Up 19 min    mem 97.23% (248.9MiB/256MiB límite)
fba_postgres       Up 19 min    mem 4.69%
fba_redis          Up 21 min    mem 2.50%
starhome-baserow   Up 3h        mem 92.74% (1.855GiB/2GiB límite)
```
`fba_pgadmin` y `starhome-baserow` están cerca de su límite de memoria — no
es una falla activa hoy, pero si crece el uso podrían empezar a fallar por
OOM. No se tocó (fuera de alcance de F15, es tuning de infra).

### Humo — media / UGC / market-intel
- **edge-tts** (`~/.venvs/media-tools`): v7.2.8, llamada de red real a
  `--list-voices` funcionó.
- **faster-whisper**: v1.2.1 importable, modelo `small` ya en caché de
  HuggingFace.
- **Remotion**: CLI 4.0.503 disponible en `~/repos/cano-video-vox`.
- Los tres siguen disponibles, no se volvió a renderizar el video completo.
- **UGC tramo gratis**: `01-products-research/discovered/*.json` (dentro de
  command-center, `01-offices/ugc-affiliate/`) sigue accesible — 4 archivos.
- **market-intel**: ver fila `cano-investment-intelligence` arriba —
  reconfirmado, 138/138 tras el fix.

### Seguridad (adelanta F16)
- `git status --short` en los 5 repos propios: limpio en 4/5 antes de
  empezar; `factory-ia-channel-v5` tenía `.factory/` y `.nexus/` untracked
  (reparado, ver §2) y `cano-investment-intelligence` tenía cambios sin
  commitear de F-anterior (reparado, ver §2).
- Permisos de `.env`: **4/5 en 600** correcto. `amazon-fba-product-hunter/.env`
  estaba en **664** (grupo+otros con lectura) — corregido a 600 (§2).
  `cano-investment-intelligence` y `ugc-commerce-studio` no tienen `.env`
  en absoluto (no es un hallazgo, simplemente no lo usan).
- Grep de patrones de llave larga (`sk-`, `AKIA`, `ghp_`, `xox[baprs]-`,
  `AIza`, bloques `PRIVATE KEY`, y un patrón genérico
  `api_key|secret|token|password` seguido de valor largo) sobre archivos
  **trackeados** de los 5 repos propios: **0 resultados** en ambos barridos.

---

## 2. Reparado — lista 2(a)

| Item | Acción | Evidencia antes/después |
|---|---|---|
| Test frágil `cano-investment-intelligence` | `tests/integration/test_cloudflare_idempotency.py`: el assert comparaba contra el literal `"body.event_id"` pero `apps/queue-consumer/src/index.ts` usa optional chaining `body?.event_id` (comportamiento correcto — prioriza event_id de dominio sobre `message.id` de transporte). Se corrigió el assert al literal real, no se tocó el código de producción. Se commitearon junto los cambios pendientes de fase anterior (`configs/upstreams/default.yaml` entrada `ai_hedge_fund`, `scripts/upstreams/clone.sh`). Push a `build/full-platform-v0.1`. | Antes: 1 fallo de 145 (baseline previo). Después: **138 passed, 0 failed** (recordar: el conteo total bajó por commits de feature intermedios, no por este fix). PR #1 (`syacreator09-sys/cano-investment-intelligence`) ya existía abierto (draft) — se comentó con el detalle del fix en vez de abrir uno duplicado. |
| `engineering-lead.yaml` → skill inexistente | Creado `skills/engineering-loop/` (manifest.json + SKILL.md) en StarHome, mismo patrón boilerplate que el resto. Decisión: crear el skill, no redirigir la referencia — los otros 3 agentes "-lead" (`forge-lead`, `content-lead`, `research-lead`) ya siguen el patrón de un skill de coordinación propio y exclusivo, distinto de los skills de los agentes que coordinan; redirigir a una combinación de `architecture-spec`+`code-review`+`testing` habría roto ese patrón (y `skills:` no admite composición). Actualizado `docs/SKILLS_MATRIX.md` marcando el hallazgo #1 como resuelto. Branch `fix/engineering-loop-skill`, PR #6 (`syacreator09-sys/cano-hermes-agentic-os`). | Antes: `python scripts/validate.py` no fallaba pero el hallazgo estaba documentado como pendiente. Después: `python -m unittest discover -s tests`: 70 OK (sin cambios, no toca lógica Python). |
| PR #5 de StarHome (`feat/finance-office`) | Rebasado sobre `main` actual (estaba a 1 commit de un merge-base viejo, `main` ya traía `cost-controller.yaml` desde F3/F11). Se completó el follow-up que el propio PR ya anticipaba: `agents/governance/cost-controller.yaml` → `agents/finance/cost-controller.yaml`, `team: governance` → `team: finance` — mismo movimiento mecánico que `budget-controller`. `agents/finance/` queda con 3 agentes sin colisión de id (`budget-controller` active, `cost-controller` candidate, `finance-controller` candidate). Force-push (con `--force-with-lease`) tras el rebase, comentario en el PR documentando el cambio. **No se mergeó** — sigue abierto para aprobación de Cano. | `python -m unittest discover -s tests`: 70 OK. `pytest -q`: 75 passed. `python scripts/validate.py`: `{"status":"ok","agents":53,"skills":58,"notes":4}`. `mergeStateStatus: CLEAN`, `mergeable: MERGEABLE`. |
| `.env.backup-0940` en StarHome | Movido (no leído, no impreso) con `mv` a `~/.secrets/backups-locales/cano-hermes-agentic-os.env.backup-0940`, permisos ajustados a 600 (directorio a 700). Ya no está en el working tree del repo. | `git status --short` en StarHome: limpio. |

### Reparaciones adicionales encontradas durante la auditoría (no en la lista original, triviales y sin ambigüedad)
- `amazon-fba-product-hunter/.env` permisos 664 → **600** (chmod directo, sin PR — cambio de permisos de archivo, no de contenido versionado).
- `factory-ia-channel-v5`: `.factory/` y `.nexus/` (salida mock de
  `tests/test_cano_completion_runtime.py`, que escribe a una ruta relativa
  literal del repo en vez de `tmp_path`) aparecían como untracked en cada
  `git status`. Añadidos a `.gitignore` junto a las entradas existentes de
  `storage/cache/` y similares — no se tocó el test en sí (cambiar su uso de
  `tmp_path` es un cambio de comportamiento de test, no un fix trivial sin
  ambigüedad). Commit directo a `main` (`a8e1098`).

---

## 3. Scoring UGC (2b) — implementado

Se implementó (no quedó pendiente). `src/ugc_commerce/product_scout_score.py`
en `ugc-commerce-studio`: función pura `score_product()`, sin llamadas de
red, sobre inputs ya estructurados. Codifica los 6 ejes documentados en
`cano-ai-command-center/01-offices/ugc-affiliate/.claude/agents/
ugc-product-scout.md` (leído como referencia, solo lectura, nada copiado):
comisión (0-25), potencial visual (0-20), fit con canal (0-20), potencial
viral (0-15), riesgo (-10 a 0, solo el peor tier aplicable), facilidad de
producción (0-10). Umbrales de decisión idénticos a la doc fuente: `>=60`
PROCEDE, `55-59` EN_ESPERA, `<55` RECHAZADO, con override duro a RECHAZADO
para claims médicos sin importar el score.

Hallazgo documentado en el propio módulo: la doc fuente llama a esto
"scoring de 100 puntos" pero sus valores por eje suman máximo 90
(25+20+20+15+10 — riesgo solo resta, nunca suma). Se codificó exactamente
lo documentado sin inventar puntos extra.

10 tests nuevos en `tests/test_product_scout_score.py` — cada tier de cada
eje, ambos umbrales de decisión, la regla de no-aditividad del riesgo, y el
override de claims médicos. Suite completa de `ugc-commerce-studio`: **21
passed** (11 preexistentes + 10 nuevos). Branch `feat/product-scout-score`,
PR #2 (`syacreator09-sys/ugc-commerce-studio`).

---

## 4. Tabla de pendientes de Cano

Consolidada en `reports/f15-pendientes-cano-2026-08-06.md` — 15 items,
ninguno intentado desde este agente (credenciales, sesiones OAuth,
decisiones de producto/seguridad).

---

## 5. Recomendación

**No converge todavía — hace falta iteración 2**, pero el trabajo que queda
es acotado. Específicamente:

1. **`test_chatwoot_hmac_skip_when_no_token` en command-center** — parece un
   bug real de código (`verify_chatwoot_hmac` devuelve `False` cuando
   debería devolver `True` sin token configurado), descubierto al correr la
   suite de `agents-platform` como validación externa. command-center es
   solo lectura desde este agente — no se puede arreglar aquí. Iteración 2
   debería, como mínimo, verificar si alguien con acceso de escritura a ese
   repo ya lo vio, y si no, escalarlo explícitamente en vez de dejarlo
   enterrado en este reporte.
2. **PR #5 y #6 de StarHome, y el PR de `ugc-commerce-studio` (#2), siguen
   sin aprobación/merge de Cano** — no es trabajo pendiente de este agente,
   pero la iteración 2 debería confirmar si ya se revisaron antes de asumir
   que el estado de `main` en cada repo sigue igual.
3. **La tabla de 15 pendientes de Cano** no se movió ni un punto en esta
   iteración (correctamente, son todos bloqueos externos) — si Cano resuelve
   alguno entre iteraciones (ej. `wrangler login`, NVIDIA Public API
   Endpoints), iteración 2 debería reconfirmar el estado y tachar lo
   resuelto.
4. Nada más en la auditoría de hoy salió roto de forma "trivialmente
   reparable sin ambigüedad" que no esté ya cubierto arriba — las 7 fallas
   de factory-v5 y las 13 fallas combinadas de command-center son 100%
   explicables por bloqueos de credenciales/infra ya documentados (o, en el
   caso del bug de Chatwoot HMAC, fuera del alcance de escritura de este
   agente).

Si Cano no tiene más credenciales/aprobaciones que aportar entre iteraciones,
la iteración 2 sería corta: reconfirmar suites, verificar merges de los 3 PRs
abiertos, y escalar el bug de Chatwoot HMAC. No hay trabajo de construcción
nuevo evidente en esta pasada.
