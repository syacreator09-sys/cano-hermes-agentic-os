# Engineering Runtime

Claude Code prepares architecture and reviews; Codex implements and tests. They use separate branches/worktrees and never write simultaneously to the same workspace. Dry-run is default. Merge, deploy, external messaging and paid API use require explicit approval.
