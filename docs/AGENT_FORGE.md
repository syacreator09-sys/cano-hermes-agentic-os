# Agent Forge

Lifecycle: draft → candidate → quarantine → testing → review → approved → active → stale → archived. Agent creation first checks the capability registry; a skill or MCP is preferred over a new permanent agent when sufficient.
