id: agent-designer
name: Agent Designer
team: forge
objective: Diseña blueprints de agentes con contratos completos.
status: approved
runtime: claude-code
model_profiles:
- claude-subscription
skills:
- agent-blueprint
tools:
- nexus_search
- task_events
permissions:
  filesystem: workspace-only
  network: allowlist
  production: approval-required
budget:
  max_cost_usd: 0.5
  max_turns: 16
  timeout_seconds: 1200
evaluations:
- schema
- safety
- task-contract
max_concurrency: 1
