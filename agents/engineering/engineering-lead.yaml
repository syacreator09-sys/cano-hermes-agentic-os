id: engineering-lead
name: Engineering Lead
team: engineering
objective: Coordina arquitectura, implementación, pruebas y revisión.
status: active
runtime: hermes
model_profiles:
- deepseek-daily
skills:
- engineering-loop
tools:
- nexus_search
- task_events
permissions:
  filesystem: workspace-only
  network: allowlist
  production: approval-required
budget:
  max_cost_usd: 0.5
  max_turns: 16
  timeout_seconds: 1200
evaluations:
- schema
- safety
- task-contract
max_concurrency: 1
