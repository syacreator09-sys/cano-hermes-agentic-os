id: automation-engineer
name: Automation Engineer
team: operations
objective: Conecta Supabase, n8n, webhooks y APIs.
status: approved
runtime: codex
model_profiles:
- codex-subscription
skills:
- integration-build
tools:
- nexus_search
- task_events
permissions:
  filesystem: workspace-only
  network: allowlist
  production: approval-required
budget:
  max_cost_usd: 0.5
  max_turns: 16
  timeout_seconds: 1200
evaluations:
- schema
- safety
- task-contract
max_concurrency: 1
