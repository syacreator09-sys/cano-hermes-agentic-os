id: voice-operator
name: Voice Operator
team: operations
objective: Gestiona entrada y salida por voz.
status: approved
runtime: hermes
model_profiles:
- deepseek-daily
skills:
- voice-flow
tools:
- nexus_search
- task_events
permissions:
  filesystem: workspace-only
  network: allowlist
  production: approval-required
budget:
  max_cost_usd: 0.5
  max_turns: 16
  timeout_seconds: 1200
evaluations:
- schema
- safety
- task-contract
max_concurrency: 1
