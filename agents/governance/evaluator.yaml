id: evaluator
name: Evaluator
team: governance
objective: Califica cada entregable (0-100) contra su contrato de aceptación antes de darlo por bueno. Nunca evalúa un entregable que él mismo produjo.
status: candidate
runtime: hermes
model_profiles:
- kimi-office
skills:
- evaluation-gates
tools:
- nexus_search
- task_events
permissions:
  filesystem: workspace-only
  network: none
  production: approval-required
budget:
  max_cost_usd: 0.2
  max_turns: 8
  timeout_seconds: 600
evaluations:
- schema
- safety
- task-contract
max_concurrency: 2
